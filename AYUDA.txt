cd d1-api
npm ci
npx wrangler d1 migrations apply finanzas_mayeson --remote
npm run deploy
cd ..

Sincronizar respaldo Sheets -> D1:
$headers = @{ "x-admin-key" = "TU_D1_ADMIN_KEY" }
Invoke-WebRequest -UseBasicParsing -Method POST -Headers $headers -Uri "https://finanzas-d1-api.mayersonm.workers.dev/api/sync/gas?chat_id=TU_CHAT_ID&limit=500" | Select-Object -ExpandProperty Content

Dashboard Inicio:
- Cerrar ciclo guarda el snapshot en D1, marca el ciclo como cerrado y deja ahorro/presupuesto sugerido para el siguiente ciclo.
- Top fugas muestra los 5 gastos variables principales del mes.

Telegram movimientos:
- cobro salario sueldo mayo 3000
- gasto supermercado arroz plaza vea 25
- gasto 25 supermercado almuerzo
- gasto 25 arroz conserva "arroz" como descripcion aunque la categoria sea otro.

Dinero Libre:
- libre
- puedo gastar 120 zapatillas
- Configura ahorro del ciclo, colchon, perfil y horizonte desde Dashboard > Config.
- Proyecta el gasto desde caja actual; deudas y fijos quedan como referencia.
- El comando libre muestra objetivo semanal si D1 lo devuelve.
- Dashboard > Calendario muestra gasto diario, fijos, deudas, cierre, pagos de credito y alertas.
- Dia 23: sugerir cerrar ciclo; el ahorro queda sugerido hasta que lo separes de verdad.

cd dashboard
npm ci
$env:VITE_GAS_API_URL="https://finanzas-d1-api.mayersonm.workers.dev/api/dashboard"
npm run build
npx wrangler pages deploy dist --project-name finanzas-dashboard --branch main
cd ..

cd apps-script
npx clasp push -f
npx clasp deploy -i AKfycby96cGWN27al7L34RA6uCfoysYjuGNcMBX3Gzx3KWM1XP-35NL2J1nYe8bxOz5UIs8j -d "actualizacion"
cd ..

git status
git add .
git commit -m "actualizacion general"
git push origin main
